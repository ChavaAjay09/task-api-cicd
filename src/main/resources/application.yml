server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/tasksdb}
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss.SSSX
    time-zone: UTC
    serialization:
      write-dates-as-timestamps: false
